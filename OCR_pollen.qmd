---


title: "Effets du pâturage sur les prairies suédoises"
group: "Bastien, Elia, Caroline, Alexia, Marine, Lily, Julie" 


bibliography: references.bib
execute: 
  freeze: auto
output: 
  html_document:
   toc: true
   toc_float: true
---

This chapter is a simple example using R

You can import R package using the code

```{r}
library(tidyverse)
library(FactoMineR)
library(dplyr)
```


and then describe the purpose of your chapter as well as executing R command.


For example a basic summary of a dataset is given by 

```{r}
df <- read.table("pollen.csv", sep = ";" , header = TRUE)
```

and produce a graph

```{r}
df %>% ggplot() +
	aes(x=species, y = body_mass_g) +
	geom_boxplot()  
```


PARTIE 3: EFFET DU PATURAGE SUR LES STRATEGIES DE POLLINISATION

```{r}
# renommer la premiere colonne pour Marine et Alexia car var treatment est ?..treatment et pose pblm
names(df)[1] <- "treatment"
names(df)
```

```{r}
# Boxplot 
par(mfrow=c(1,2))
boxplot(df$RatioPoll~df$treatment, varwidth = TRUE, ylab = "Ratio Pollinisation", xlab = "treatment", col='blue', main = "")

```
On observe pas de differences significatives entre les parcelles qui subissent du paturage et celles qui n'en subissent pas d'apres le boxplot.

```{r}
# Anova
poll1<-lm(RatioPoll~1,data=df)
poll2<-lm(RatioPoll~treatment,data=df)
anova(poll1,poll2)
```

 la P-value n'est pas significative au seuil de 5%, on rejette donc l'hypothèse que le pâturage à un effet sur RatioPoll.Le test Anova confirme notre observation graphique:il n'y a pas de difference entre les parcelles patures et celles naturelles.

```{r}
poll3<-LinearModel(RatioPoll~(treatment+season+year+observer+R.score+S.score+C.score)*
                     (treatment+season+year+observer+R.score+S.score+C.score),data=df,selection = "aic")
poll3
```
Le modele proposé par R a bien un BIC plus faible que notre modele initiale. Cependant, certaines interactions gardées par R n'ont pas d'effet significatif. Nous avons decide de tester un modele sans celle ci. 

```{r}
poll4<-LinearModel(RatioPoll~( treatment + season + year + 
    observer + R.score + S.score + C.score + treatment:R.score + 
    treatment:C.score + year:C.score + 
    observer:R.score + R.score:C.score),data=df)
poll4
```
On a observe que l'interaction R.score:C.score n'est pas significative, i=on teste un nouveau modele sans elle.
```{r}
poll5<-LinearModel(RatioPoll~( treatment + season + year + 
    observer + R.score + S.score + C.score + treatment:R.score + 
    treatment:C.score + year:C.score + 
    observer:R.score ),data=df)
poll5
```
On enlève R.score car c'est le moins significatif 

```{r}
poll6<-LinearModel(RatioPoll~( season + C.score + treatment:R.score + 
    treatment:C.score + year:C.score ),data=df)
poll6
```
